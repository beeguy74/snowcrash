Dump file level02.pcap in the home directory of level02.

A PCAP (Packet Capture) file is a snapshot of live network traffic, recording the exact data packets flowing across a network interface at a specific moment in time
It is not a log file or a summary; it is the literal sequence of bytes that traveled over the wire.

One can analyse it with wireshark GUI to get flag02 password:
```
- Right-click on any packet in a TCP conversation
- Select "Follow → TCP Stream"
```
